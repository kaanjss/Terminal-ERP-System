import terminal_framework


def execute():
	terminal_framework.reload_doctype("Payment Entry")
	terminal_framework.db.sql(
		"""update `tabPayment Entry` set status = CASE
		WHEN docstatus = 1 THEN 'Submitted'
		WHEN docstatus = 2 THEN 'Cancelled'
		ELSE 'Draft'
		END;"""
	)
